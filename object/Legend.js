export default class Legend {
  // eslint-disable-next-line max-len
  constructor(name, life, move, speed, att, def, compPas, compDef, compSp1, compSp2, compUlt) {
    this.name = name;
    this.maxLife = life;
    this.maxMove = move;
    this.speed = speed;
    this.att = att;
    this.def = def;
    this.compPas = compPas;
    this.compDef = compDef;
    this.compSp1 = compSp1;
    this.compSp2 = compSp2;
    this.compUlt = compUlt;
  }
  useDefaultAttak(cible) {

  };
  useCompOne(cible) {

  };
  useCompTwo(cible) {

  };
  useUltimate(cible) {

  };
  getDamage() {

  };
  moveLegend() {

  };
  initBattel(pos) {

  };
}
